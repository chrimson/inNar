services:
  innar:
    image: chrimson/innar:latest
    ports:
      - 5000:5000
    volumes:
      - ./story.json:/app/story.json
    networks:
      - charlie_default
    restart: always

networks:
  charlie_default:
    external: true
